import { SafeAreaView } from "react-native";
import { StatusBar } from "expo-status-bar";
import { FlatList, StyleSheet, Text, View } from "react-native";

export default function AddPlanet({ id }) {
  return (
    <SafeAreaView>
      <Text> Add a planet </Text>
    </SafeAreaView>
  );
}
